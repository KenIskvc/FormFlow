<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="FormFlow.MobileApp.VideoManagementPage"
             Title="Videos">

    <ScrollView>
        <VerticalStackLayout Spacing="20" Padding="25">

            <Label Text="Video Management"
                   FontSize="26"
                   FontAttributes="Bold" />

            <Label Text="Manage your uploads: rename, delete, or start a new analysis."
                   FontSize="15"
                   TextColor="Gray" />

            <BoxView HeightRequest="1" Color="LightGray" Margin="0,5" />

            <CollectionView x:Name="VideosListView">
                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Border Margin="0,8" 
                                Padding="15" 
                                StrokeShape="RoundRectangle 12" 
                                BackgroundColor="#F5F5F5">

                            <Grid ColumnDefinitions="*, Auto, Auto, Auto">

                                <VerticalStackLayout Grid.Column="0" VerticalOptions="Center">
                                    <Label Text="{Binding FileName}" 
                                           FontSize="16" TextColor="Black"
                                           FontAttributes="Bold" />
                                    <Label Text="{Binding UploadDate}" 
                                           FontSize="12" 
                                           TextColor="Gray" />
                                </VerticalStackLayout>

                                <Button Grid.Column="1" 
                                        Text="Re-analyze" 
                                        Clicked="OnReAnalyzeClicked" 
                                        Margin="5,0" 
                                        BackgroundColor="#4CAF50" 
                                        TextColor="White"
                                        HeightRequest="40" />

                                <Button Grid.Column="2" 
                                        Text="Rename" 
                                        Clicked="OnRenameClicked" 
                                        Margin="5,0" 
                                        BackgroundColor="#2196F3" 
                                        TextColor="White"
                                        HeightRequest="40" />

                                <Button Grid.Column="3" 
                                        Text="Delete" 
                                        Clicked="OnDeleteClicked" 
                                        BackgroundColor="#FF5252" 
                                        TextColor="White"
                                        HeightRequest="40" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>
</ContentPage>